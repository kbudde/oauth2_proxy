dependencies:
  post:
    - find . -ls || true
    - ls -lR
    - ls -l oauth2_proxy || true
    - ldd oauth2_proxy || true

test:
  post:
    - mkdir $CIRCLE_ARTIFACTS/go_binaries
    - mv oauth2_proxy $CIRCLE_ARTIFACTS/go_binaries
